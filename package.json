{
  "name": "google-inbox-enhanced",
  "version": "0.0.1",
  "description": "Google Inbox Enhanced",
  "scripts": {
    "compile": "babel src --out-dir dist --source-maps",
    "watch": "nodemon --ignore dist --exec \"npm run compile\"",
    "start": "npm run compile"
  },
  "author": "Richard van der Dys",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel": "^5.8.34",
    "nodemon": "^1.8.1",
    "babel-eslint": "*"
  }
}
